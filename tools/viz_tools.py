from __future__ import annotations

from typing import Any, Dict

import pandas as pd


def empty_visual_bundle(_df: pd.DataFrame) -> Dict[str, Any]:
    """Placeholder for visualization generation.

    Keeps the scaffolding lightweight; real charting can be added later.
    """
    return {"plots": []}

